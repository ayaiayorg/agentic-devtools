---
description: "Setup Worktree: Set up a git worktree in the background"
---

## User Input

```text
$ARGUMENTS
```

You **MUST** consider the user input before proceeding (if not empty).

## Purpose

Set up a git worktree in the background (internal command).

## Prerequisites

No prerequisites required.

## Actions

1. Run the command:

   ```bash
   agdt-setup-worktree-background
   ```

## Expected Outcome

A worktree is created and configured (background task).

## Next Step

Command is complete.
