# Tasks: Remove Invalid --yes Flag and Change Default Merge Strategy to Rebase

**Feature Branch**: `fix/1215/remove-yes-flag-rebase-default`
**Source Issue**: [#1215](https://github.com/ayaiayorg/agentic-devtools/issues/1215)

## User Story Map

| Label | Story | Priority |
|-------|-------|----------|
| US1 | Merge a PR Successfully Without the Invalid Flag | P1 |
| US2 | Default Merge Strategy Is Rebase | P2 |
| US3 | Documentation and Internal References Reflect New Defaults | P3 |

## Phase 1: Setup

- [ ] T001 Create feature branch `fix/1215/remove-yes-flag-rebase-default` from `main`

## Phase 2: Foundational

_No foundational tasks — all changes map directly to user stories._

## Phase 3: US1 — Remove Invalid `--yes` Flag (P1)

- [ ] T002 [US1] Update test assertion from `assert "--yes" in cmd` to `assert "--yes" not in cmd` in `tests/unit/cli/github/pr_merge/test__execute_merge.py` (line 22) — RED: test fails against
  current source
- [ ] T003 [US1] Remove `"--yes"` from the command list in `_execute_merge()` in `agentic_devtools/cli/github/pr_merge.py` (line 73) — GREEN: test passes
- [ ] T004 [US1] Run focused tests to verify: `agdt-test-pattern tests/unit/cli/github/pr_merge/test__execute_merge.py -v`

## Phase 4: US2 — Change Default Merge Strategy to Rebase (P2)

- [ ] T005 [US2] Update `test_default_strategy_is_squash` → `test_default_strategy_is_rebase` and change assertion from `"squash"` to `"rebase"` in `tests/unit/cli/github/pr_merge/test_merge_pr.py`
  (lines 34, 151, 153, 167) — RED: tests fail against current source
- [ ] T006 [P] [US2] Update all four default-strategy assertions from `"squash"` to `"rebase"` in `tests/unit/cli/github/pr_merge/test_pr_merge_command.py` (lines 25, 42, 68, 121) — RED: tests fail
  against current source
- [ ] T007 [US2] Change `_DEFAULT_STRATEGY = "squash"` to `_DEFAULT_STRATEGY = "rebase"` in `agentic_devtools/cli/github/pr_merge.py` (line 22) — GREEN (partial)
- [ ] T008 [US2] Change `merge_pr` function signature default from `strategy: str = "squash"` to `strategy: str = _DEFAULT_STRATEGY` in `agentic_devtools/cli/github/pr_merge.py` (line 120) — GREEN
  (partial)
- [ ] T009 [US2] Update argparse help text from `"Merge strategy (default: squash)"` to `"Merge strategy (default: rebase)"` in `agentic_devtools/cli/github/pr_merge.py` (line 286) — GREEN: all tests
  pass
- [ ] T010 [US2] Run focused tests to verify: `agdt-test-pattern tests/unit/cli/github/pr_merge/ -v`

## Phase 5: US3 — Documentation Updates (P3)

- [ ] T011 [P] [US3] Update command mapping table: `gh pr merge --squash --delete-branch` → `gh pr merge --rebase --delete-branch` in `.github/copilot-instructions.md` (line 160)
- [ ] T012 [P] [US3] Update parameter docs: `default squash` → `default rebase` in `.github/copilot-instructions.md` (line 180)
- [ ] T013 [P] [US3] Update tooling priority table: `gh pr merge --squash` → `gh pr merge --rebase` in `.github/prompts/agdt.pr-merge-manager.prompt.md` (line 21)

## Phase 6: Polish & Cross-Cutting

- [ ] T014 Run full test suite: `agdt-test` + `agdt-task-wait`
- [ ] T015 Run full PR checks: `bash scripts/run-pr-checks.sh`
- [ ] T016 Grep validation — confirm zero hits for stale `"--yes"` in source: `grep -r '"--yes"' agentic_devtools/`
- [ ] T017 Grep validation — confirm zero hits for stale squash defaults in source:

  ```bash
  grep -rn '_DEFAULT_STRATEGY.*squash\|default: squash\|strategy.*=.*"squash"' agentic_devtools/cli/github/pr_merge.py
  ```

- [ ] T018 Commit and push via `agdt-git-save-work`

## Dependencies

```text
T002 → T003 (TDD red-green)
T003 → T004 (verify fix)
T005 → T007 (TDD red-green)
T006 → T007 (TDD red-green)
T007 → T008 → T009 (sequential source edits)
T009 → T010 (verify defaults)
T004, T010 → T014 (full suite after all code changes)
T011, T012, T013 — parallel, no code dependencies
T014 → T015 → T016 → T017 → T018 (sequential validation chain)
```

---
_Generated by Copilot SDK (claude-opus-4.6)_
