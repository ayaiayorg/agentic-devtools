# Specification Quality Checklist: Remove Invalid --yes Flag from gh pr merge and Change Default Strategy to Rebase

**Purpose**: Validate specification completeness before proceeding to planning
**Created**: 2026-04-21
**Feature**: [spec.md](../spec.md)
**Source Issue**: #1215

## Content Quality

- [ ] CHK001 User value is stated clearly and in user-facing terms, not only as an implementation fix for the broken `--yes` flag
- [ ] CHK002 User stories are present in the spec body and use clear "As a … I want … so that …" phrasing aligned to the feature scope
- [ ] CHK003 Priority is assigned to each listed requirement (FR-001, FR-007, FR-008, NFR-003) or the spec explicitly states that all requirements share the same priority
- [ ] CHK004 Clarification 1 ("strategy flags alone bypass interactive confirmation") remains behavioral and does not over-prescribe implementation details in FR-001

## Requirement Completeness

- [ ] CHK005 Each user story includes explicit, testable acceptance conditions rather than relying only on changelog or summary wording
- [ ] CHK006 Edge case for `--no-delete-branch` interaction with rebase strategy is documented with an explicit scenario showing the flag is preserved when strategy changes from squash to rebase
- [ ] CHK007 Acceptance scenarios cover at least: (a) merge with `--rebase` succeeding without `--yes`, (b) merge with explicit `--squash` override, and (c) merge with explicit `--merge` override
- [ ] CHK008 NFR-003 includes a concrete measurable criterion in the spec body and that criterion is specific enough to validate during implementation or testing
- [ ] CHK009 Scope boundaries explicitly distinguish out-of-scope repo-level
  "squash-merge" wording from in-scope CLI documentation updates, so references
  such as `.github/copilot-instructions.md` and
  `.github/prompts/agdt.pr-merge-manager.prompt.md` remain in scope when they
  describe the `agdt-gh-pr-merge` default strategy or `--yes` removal
- [ ] CHK010 Dependency on `gh` CLI behavior (strategy flags implicitly bypassing confirmation) is linked to a specific `gh` CLI version, reproducible observation, or documentation reference

## Feature Readiness

- [ ] CHK011 FR-001, FR-007, and FR-008 each include clear pass/fail acceptance criteria traceable to the described behavior change
- [ ] CHK012 The test files mentioned in clarification 3 map cleanly to user-observable scenarios: current broken behavior, expected behavior without `--yes`, and explicit strategy override behavior
- [ ] CHK013 Success criteria remain measurable and include:
  (a) `agdt-gh-pr-merge --strategy rebase` completes with exit code 0,
  (b) `_DEFAULT_STRATEGY` reads `"rebase"` not `"squash"`, and
  (c) no `--yes` appears in any constructed `gh pr merge` command
- [ ] CHK014 Clarification 5 ("command was broken anyway due to `--yes`") is captured as background or motivation context rather than as a normative requirement

## Notes

- This checklist was updated to align with the current specification content for issue #1215
- Items marked incomplete identify remaining clarity, traceability, or testability gaps before proceeding to planning
- The spec includes user stories, acceptance scenarios, and measurable requirement content; this checklist now validates their quality and completeness rather than treating them as missing
- Source analysis confirms `--yes` at `pr_merge.py:73` and `_DEFAULT_STRATEGY = "squash"` at `pr_merge.py:22` are the two primary code locations requiring change
- Test file `test__execute_merge.py:22` asserts `"--yes" in cmd` — this assertion must be removed or inverted to match the corrected behavior

---
*Generated by Copilot SDK (claude-opus-4.6)*
