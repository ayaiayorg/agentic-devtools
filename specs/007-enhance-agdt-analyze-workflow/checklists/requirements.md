# Specification Quality Checklist: Enhance agdt.analyze-workflow

**Purpose**: Validate specification completeness before proceeding to planning
**Created**: 2026-04-10
**Feature**: [spec.md](../spec.md)
**Source Issue**: #1179

## Content Quality

- [ ] CHK001 Each of the 5 user stories articulates user value — not just "add parameter X" but why the user needs it
      (e.g., why `--issue-key` vs `--pr-id` matters for the analyst's workflow)
- [ ] CHK002 All 5 user stories follow "As a [role], I want [goal], so that [benefit]" format and reference concrete
      agdt-analyze-workflow capabilities (multi-identity scanning, static-only mode, external_context output, etc.)
- [ ] CHK003 Each of the 16 functional requirements has an explicit priority (must/should/could) — especially the
      mutual exclusion rule for `--issue-key` + `--pr-id` and the resolution precedence table
- [ ] CHK004 Functional requirements describe *what* the system does (e.g., "scan all identity directories") without
      prescribing *how* it is implemented (no function signatures, class names, or data structure choices in requirements)

## Requirement Completeness

- [ ] CHK005 Each user story has at least one testable acceptance criterion — verify the multi-identity log scanning
      story specifies the `[identity: {name}]` prefix format as an observable outcome
- [ ] CHK006 All 7 edge cases are documented with exact error messages — confirm coverage of: missing state keys,
      invalid `--issue-key`/`--pr-id` combination, no log files found, empty identity directories, inaccessible
      external worktrees, nonexistent repo path, and the static-only mode boundary
- [ ] CHK007 Acceptance scenarios use Given/When/Then or equivalent structured format — especially for the
      `--issue-key` + `--pr-id` mutual exclusion error path and the read-only safety guarantee for external worktrees
- [ ] CHK008 All 5 NFRs have measurable thresholds — verify backward compatibility defines what "compatible" means
      (existing JSON schema fields unchanged), latency specifies a concrete target, read-only safety defines the
      enforcement mechanism, error clarity defines minimum content, and determinism defines reproducibility scope
- [ ] CHK009 Out-of-scope section explicitly excludes: modifying external worktree state, adding new bug taxonomy
      categories, changing the existing JSON output schema (beyond the new `external_context` field), and writing to
      directories outside the caller's state directory
- [ ] CHK010 Dependencies section lists: `state.py`'s `_sync_bootstrap_for_context_key()` chain for resolution
      precedence, the existing SKILL.md analysis schema, background-tasks log directory conventions, and the
      `.agdt/workflows/{identity}/{worktree_key}/` directory structure

## Feature Readiness

- [ ] CHK011 All 16 functional requirements — especially parameter handling, mutual exclusion, resolution precedence,
      source analysis scope, and `external_context` data model — have corresponding acceptance criteria that can be
      verified in isolation
- [ ] CHK012 User scenarios cover the full usage matrix: issue-key-only invocation, pr-id-only invocation,
      both-parameters error, static-only mode (no external worktree), multi-identity log collection across identities,
      and cross-repo source-vs-log distinction
- [ ] CHK013 All 6 success criteria include verification methods — confirm they specify concrete checks such as: JSON
      output validates against schema, `external_context` field is populated/null as expected, identity attribution is
      present in log evidence, error messages match documented edge case text, and existing output fields remain
      unchanged
- [ ] CHK014 The `external_context` data model, resolution precedence table, and `[identity: {name}]` prefix format
      are specified as behavioral contracts in requirements — not as implementation-level code structures leaking into
      the spec

## Notes

- This checklist was generated from the specification content for issue #1179
- The spec.md now contains the full specification content including user stories, functional requirements
  (FR-001–FR-016), non-functional requirements (NFR-001–NFR-005), edge cases, acceptance criteria,
  success criteria, dependencies, and out-of-scope section
- Items marked incomplete require spec updates before proceeding to planning
*Generated by Copilot SDK (claude-opus-4.6)*
