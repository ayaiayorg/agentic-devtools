# Implementation Plan: Update Docs & Prompts for 4-Tier CI and Pre-Push Hooks

**Issue**: [#1657](https://github.com/ayaiayorg/agentic-devtools/issues/1657)

## Technical Context

- **Repository**: `ayaiayorg/agentic-devtools` — a pip-installable Python CLI package
- **Tooling**: `ruff` (sole linter/formatter), `mypy`, `markdownlint-cli2`, `bandit`
- **CI**: 4-tier pipeline — 3 PR-blocking gates + 1 post-merge suite
- **Pre-push hook**: `.githooks/pre-push` runs targeted checks on changed files
- **Test runner**: `agdt-test` / `agdt-task-wait` (never bare `pytest`)
- **Commit policy**: Single commit per PR via `agdt-git-save-work`

## Research Summary

See [research.md](research.md) — key decision: pure documentation change, no code modifications needed. Validation via `markdownlint-cli2` on all four files.

## Design Overview

This is a documentation-only change across 4 files:

```text
.github/agents/senior-python-developer.md    — tooling section (lines 539-568)
.github/agents/copilot-instructions.md       — entire file rewrite
.github/prompts/agdt.address-copilot-review.ci-repair.prompt.md         — Phase 3b intro
.github/prompts/agdt.address-copilot-review.evaluate-and-respond.prompt.md  — Phase 5 intro
```

No new files created. No code changes. No test changes required.

## Implementation Phases

### Phase 1: Update `senior-python-developer.md` (FR-001, FR-002)

**Deliverable**: Accurate tooling section with ruff-only guidance and pre-push hook documentation.

**Changes (lines 539–568)**:

1. Line 545: Remove `(or black + flake8 + isort)` — change to just `**Linting**: ruff`
2. Lines 552–568: Replace the Code Quality Tools code block:
   - Remove `black .` and `isort .`
   - Add `ruff format .` (formatting)
   - Keep `ruff check .` (linting)
   - Keep `mypy src/` (type checking)
   - Keep `pytest --cov=src --cov-report=html` (generic — this is a generic agent, not repo-specific)
   - Keep `bandit -r src/` (security)
3. After the code block, add a new subsection documenting:
   - Pre-push hook automation (what it runs)
   - Recovery guidance: "If push is rejected, fix issues, amend commit, retry"

### Phase 2: Rewrite `copilot-instructions.md` (FR-003)

**Deliverable**: Comprehensive repo-specific development guidelines.

**Changes (entire file)**:

1. Remove `cd src` — commands run from repo root
2. Replace `pytest` with `agdt-test` / `agdt-task-wait`
3. Replace `ruff check .` with both `ruff format .` and `ruff check .`
4. Add CI Pipeline section documenting:
   - 4-tier overview
   - 3 PR-blocking gates with names and coverage
   - Post-merge full suite (non-blocking)
5. Add Pre-Push Hook section documenting automation
6. Note that agents don't need to manually lint/format — hook handles it
7. Keep the `<!-- MANUAL ADDITIONS -->` markers

### Phase 3: Update `ci-repair.prompt.md` (FR-004)

**Deliverable**: Phase 3b gains an introductory note listing CI gate names.

**Changes (insert after line 133, before "### For Lint Failures")**:

1. Add a gate reference block listing:
   - `Targeted Checks ✅` — ruff format, ruff check, markdownlint, per-file coverage, mypy, test structure
   - `Smart Module Tests ✅` — smart module-level test execution
   - `Copilot Review ✅` — automated Copilot code review gate
2. Add note: lint failures reaching CI may indicate pre-push hook bypassed with `--no-verify`

### Phase 4: Update `evaluate-and-respond.prompt.md` (FR-005)

**Deliverable**: Phase 5 gains an introductory note listing CI gate names.

**Changes (insert after line 186, before "### For Lint Failures")**:

1. Same gate reference block as Phase 3
2. Same note about `--no-verify` bypass

### Phase 5: Validation (NFR-001, NFR-002)

**Deliverable**: All files pass markdownlint; no out-of-scope files modified.

1. Run `markdownlint-cli2` on all four updated files
2. Verify no changes to Jira workflow agents, `agdt.git-*`, or `agdt.test*` files
3. Run `git diff --stat` to confirm only the 4 target files are modified

## Risk Assessment

| Risk | Likelihood | Impact | Mitigation |
|------|-----------|--------|------------|
| Markdownlint violations in new content | Medium | Low | Run linter after each file edit |
| Accidentally modifying out-of-scope files | Low | Medium | Explicit scope check in Phase 5 |
| Gate names don't match actual CI workflow names | Low | High | Cross-reference with `.github/workflows/` YAML |

## Dependencies

- **Internal**: `.githooks/pre-push` script (already merged via PR #1650)
- **Internal**: GitHub Actions workflow files defining the 3 gate names
- **External**: None — pure documentation change

---
*Generated by Copilot SDK (claude-opus-4.6)*
