# Specification Quality Checklist: create-jira-issue workflow: no Copilot session started after placeholder creation and worktree setup

**Purpose**: Validate specification completeness before proceeding to planning
**Created**: 2026-06-07
**Feature**: [spec.md](../spec.md)
**Source Issue**: #1741

## Content Quality

- [ ] CHK001 All three user stories focus on developer-facing value (automatic continuation, truthful messaging, path parity) rather than internal implementation mechanics
- [ ] CHK002 User Stories 1–3 follow "As a developer… I want… so I can…" format with clear motivation clauses
- [ ] CHK003 Priority assignments are justified: US-1 (Primary Workflow) and US-2 (Error Recovery) are P1; US-3 (Graceful Degradation / path parity) is P2
- [ ] CHK004 FR-001 through FR-005 describe observable behaviors (`perform_auto_setup()` call, truthful output, degraded message content) without prescribing internal code structure beyond necessary
  interface contracts

## Requirement Completeness

- [ ] CHK005 Each acceptance scenario in US-1, US-2, and US-3 uses Given/When/Then format with observable, testable outcomes (e.g., "continuation guidance appears automatically")
- [ ] CHK006 Partial failure scenario is documented: FR-005 specifies behavior when `perform_auto_setup()` returns `False` after successful placeholder creation (no rollback, emit manual command, exit
  code 1)
- [ ] CHK007 All six acceptance scenarios across US-1, US-2, and US-3 use consistent Given/When/Then structure with specific trigger conditions and expected outcomes
- [ ] CHK008 SC-001 defines measurable success (100% of successful setups auto-display guidance); SC-002 defines zero mismatches between claims and behavior; SC-003 defines equivalent UX across both
  paths
- [ ] CHK009 Scope boundaries are explicit: NFR-001 excludes async Copilot session startup from the 120-second budget; NFR-002 scopes backward compatibility to three specific contracts (utility
  function, issue-key path, CLI parameters)
- [ ] CHK010 Dependencies are identified: relies on `perform_auto_setup()`, `runOn: folderOpen` task injection via
  `agdt-copilot-auto-start`, `_start_copilot_session_for_workflow()` as the secondary fallback inside the background setup task,
  and `@agdt.get-next-workflow-prompt` — all referenced with expected behavior

## Feature Readiness

- [ ] CHK011 FR-001 through FR-005 each have traceable acceptance criteria via the user story scenarios (FR-001/FR-002/FR-003 → US-1 and US-3; FR-004/FR-005 → US-2)
- [ ] CHK012 All three user scenarios cover the key paths: happy path without `--issue-key` (US-1), auto-setup failure after placeholder creation (US-2), and parity between `--issue-key` and
  no-issue-key paths (US-3)
- [ ] CHK013 Success criteria SC-001 ("100% of successful setups"), SC-002 ("zero observed mismatches"), and SC-003 ("equivalent continuation UX") are measurable and verifiable through integration
  testing
- [ ] CHK014 Spec references `perform_auto_setup()`, `create_placeholder_and_setup_worktree()`, and `runOn: folderOpen` as interface contracts only — the Clarifications section explicitly delegates
  internal restructuring decisions (retain utility, caller responsibility) without dictating code layout

## Notes

- This checklist was generated from the specification content for issue #1741
- Items marked incomplete require spec updates before proceeding to planning

---
*Generated by Copilot SDK (claude-opus-4.6)*
