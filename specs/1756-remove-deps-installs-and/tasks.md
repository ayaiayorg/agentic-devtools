# Tasks: Remove --no-deps Installs and Make github-copilot-sdk a Direct Dependency

**Issue**: [#1756](https://github.com/ayaiayorg/agentic-devtools/issues/1756)

## Phase 1: Setup

- [ ] T001 Create feature branch from main for #1756 implementation

## Phase 2: Foundational — Dependency Declaration

- [ ] T002 [US1] [FR-001, FR-007] Add `"github-copilot-sdk>=0.1.0,<1.0.0"` to `[project.dependencies]` in `pyproject.toml`
- [ ] T003 [US1] [FR-002] Remove the `copilot-sdk` optional-dependencies group from `pyproject.toml` (leave `langchain` and `dev` groups unchanged)
- [ ] T004 [US1] Reinstall package with `pip install -e .` and verify `from copilot import CopilotClient` succeeds in a clean context

## Phase 3: User Story 1 — Simplified CI Workflow Installation (P1)

- [ ] T005 [US1] [FR-003] Simplify `.github/workflows/ai-pr-loop.yml` install step (lines 62–66): remove separate SDK install, `--force-reinstall`, `--no-deps`; retain `pip install --upgrade pip`,
  `pip install agentic-devtools`, and optional SDK import smoke check
- [ ] T006 [P] [US1] [FR-004] Simplify `.github/workflows/speckit-phase-progression.yml` install step (lines 483–487): remove separate SDK install, `--force-reinstall`, `--no-deps` (including
  `--no-deps` on `pip install .`); retain `pip install --upgrade pip`, `pip install .`, and optional SDK import smoke check

## Phase 4: User Story 2 — Early Detection of SDK Incompatibilities (P1)

- [ ] T007 [US2] Verify that the version constraint `>=0.1.0,<1.0.0` in `[project.dependencies]` (from T002) enables pip resolver to catch incompatible SDK versions at install time — no additional
  code changes required (covered by FR-001/FR-007)

## Phase 5: User Story 3 — Simplified Runtime Import Handling (P2)

- [ ] T008 [US3] [FR-005] Remove the entire defensive diagnostic block (lines 31–56) from `.github/scripts/speckit-trigger/copilot_generate.py` — replace with simplified try/except that falls back to
  `copilot.config.SubprocessConfig` and lets `ImportError` propagate naturally
- [ ] T009 [US3] [FR-005] Remove `import subprocess` from `.github/scripts/speckit-trigger/copilot_generate.py` if no longer used elsewhere in the file
- [ ] T010 [US3] [FR-006] Remove `TestImportFailurePath.test_import_failure_exits_with_status_1_and_prints_diagnostics` from `tests/workflows/test_copilot_generate.py` (asserts on removed `pip show`
  diagnostic output)
- [ ] T011 [P] [US3] [FR-006] Remove `TestImportFailurePath.test_import_failure_warns_about_conflicting_copilot_package` from `tests/workflows/test_copilot_generate.py` (asserts on removed
  conflicting-package detection)
- [ ] T012 [US3] [FR-006] Add new test in `tests/workflows/test_copilot_generate.py` verifying that when SDK import fails, `ImportError` propagates naturally (no `sys.exit(1)`, no subprocess calls)

## Phase 6: User Story 4 — Consistent Local Development Experience (P3)

- [ ] T013 [US4] Verify that `pip install -e ".[dev]"` in a clean venv installs `github-copilot-sdk` automatically — no code changes required (covered by dependency promotion in T002)

## Phase 7: Polish & Cross-Cutting

- [ ] T014 [FR-008] Add CHANGELOG.md entry under `[Unreleased]` → `### Changed` documenting: removal of `copilot-sdk` optional extra, SDK now a direct dependency, migration guidance for `pip install
  agentic-devtools[copilot-sdk]` users
- [ ] T015 [FR-006] Run full test suite (`agdt-test` + `agdt-task-wait`) and verify 100% pass rate with no diagnostic-related assertion failures
- [ ] T016 Run `bash scripts/targeted-checks.sh` — lint, format, type-check must pass
- [ ] T017 Grep all `.github/workflows/` files for residual `--no-deps`, `--force-reinstall`, or separate `github-copilot-sdk` install lines to confirm no other workflows are affected
- [ ] T018 Run `pip check` after install to verify no transitive dependency conflicts introduced by the SDK

## Dependencies

```text
T001 → T002, T003
T002, T003 → T004
T004 → T005, T006 (parallel)
T004 → T007
T008, T009 → T010, T011 (parallel)
T010, T011 → T012
T012 → T015
T005, T006, T012, T014 → T015
T015 → T016
T016 → T017, T018 (parallel)
```

## FR Coverage Matrix

| FR | Tasks |
| --- | --- |
| FR-001 | T002 |
| FR-002 | T003 |
| FR-003 | T005 |
| FR-004 | T006 |
| FR-005 | T008, T009 |
| FR-006 | T010, T011, T012, T015 |
| FR-007 | T002 |
| FR-008 | T014 |

---
*Generated by Copilot SDK (claude-opus-4.6)*
