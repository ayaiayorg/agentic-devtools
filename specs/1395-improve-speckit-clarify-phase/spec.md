# Spec: Improve SpecKit clarify phase retry logic to better resolve structural validation errors

- **6 user stories** (3×P1, 2×P2, 1×P3) covering: targeted section insertion retries, requirement count preservation, multi-layer escalation, configurable budgets, structured logging, and heading
  preservation
- **19 functional requirements** + **5 non-functional requirements** — grounded in the existing codebase patterns (markdownlint 5-layer, CRITICAL gate 2-layer, FR validation retry)
- **Multi-layer architecture**: Layer 1 (failure-type-specific prompt augmentation) → Layer 2 (enhanced self-validation prompt strategy), each with independent configurable retry budgets
- **Default budget doubles** from 2 to 4 attempts; backward compatible with `FR_VALIDATION_MAX_RETRIES`
- **5 edge cases** documented, including co-occurring failures and zero-retry configuration
- **0 `[NEEDS CLARIFICATION]` markers** — all requirements are deterministic based on the issue and existing patterns

---
*Generated by Copilot SDK (claude-opus-4.6)*
