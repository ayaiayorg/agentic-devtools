# Specification Quality Checklist: SpecKit prompts: Require language specifier on fenced code blocks

**Purpose**: Validate specification completeness before proceeding to planning
**Created**: 2026-04-15
**Feature**: [spec.md](../spec.md)
**Source Issue**: #1194

## Content Quality

- [ ] CHK001 Each user story (including User Story 4 covering agent files and pipeline script) articulates user value —
  not just what is built, but why it matters to the template author or CI pipeline consumer
- [ ] CHK002 All user stories follow "As a [role], I want [goal], so that [benefit]" format
- [ ] CHK003 FR-001 through FR-008 are assigned priority levels (must/should/could) so implementers know which are blocking vs. deferrable
- [ ] CHK004 Requirements (FR-001 through FR-008) describe *what* the system must do without prescribing unnecessary internal code structure —
  treat FR-001's `cat`-based injection as an intentional hard constraint, and verify that FR-008's per-template placement instructions
  (and any other requirement wording) are framed in terms of observable behaviour rather than incidental implementation internals

## Requirement Completeness

- [ ] CHK005 Every functional requirement (FR-001 through FR-008) has at least one testable acceptance scenario —
  verify FR-005 (`.specify/memory/markdown-rules.md` shared location) and FR-008 (per-template placement) are independently verifiable
- [ ] CHK006 The edge case for a missing `.specify/memory/markdown-rules.md` shared file specifies the expected graceful degradation
  behaviour: warn to stderr, continue execution, and skip injection, with enough detail to write a test
- [ ] CHK007 Acceptance scenarios (including SC-006 for pipeline script verification) follow Given/When/Then or equivalent structured format with concrete inputs and expected outputs
- [ ] CHK008 NFR-001's measurable limits (500-char shared file content, reference line length ≤ 100 chars, 600-char combined limit)
  are stated as precise pass/fail thresholds with exact numeric bounds
- [ ] CHK009 Scope boundaries explicitly state what is out of scope —
  confirm whether non-SpecKit templates, non-fenced-code markdown constructs, and runtime validation of language specifier correctness are excluded
- [ ] CHK010 Dependencies and assumptions are documented — verify assumptions about which templates currently load the constitution,
  the existence of the `.specify/memory/` directory, and `cat` availability in the pipeline environment

## Feature Readiness

- [ ] CHK011 FR-005 (shared file location), FR-008 (per-template placement for all 9 templates), and FR-001 (pipeline `cat` injection)
  each have explicit acceptance criteria — not just a description of what changed
- [ ] CHK012 User scenarios cover all three integration surfaces: command templates (explicit load instruction),
  agent files (same one-line load instruction), and the pipeline script (`cat` injection) — no surface is only mentioned in passing
- [ ] CHK013 Success criteria include at least one measurable outcome per integration surface —
  e.g., "all 9 templates include the load instruction", "pipeline script injects shared file content under 600 chars",
  "generated artifacts produce zero MD040 violations in `markdownlint-cli2`", and
  "missing file triggers graceful degradation without build failure"
- [ ] CHK014 The spec does not embed implementation details such as specific Python function signatures, file I/O code,
  or template engine internals — verify FR-008's "per-template placement instructions" describe expected behaviour
  observable from the template output, not internal template engine calls

## Notes

- This checklist was generated from the specification content for issue #1194
- Items marked incomplete require spec updates before proceeding to planning
- Review this checklist against the current `spec.md` content.
- Any CHK item failures should be based on gaps or ambiguities that still exist in the present document,
  not on an earlier draft characterization of the specification structure.

---
*Generated by Copilot SDK (claude-opus-4.6)*
