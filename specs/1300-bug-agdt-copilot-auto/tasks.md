# Tasks: Windows [WinError 32] Retry for agdt-copilot-auto-start

**Source Issue**: #1300

## Dependencies

- T002 → T001
- T003 → T001
- T004 → T002, T003
- T005 → T003
- T006 → T003
- T007 → T003
- T008 → T003
- T009 → T003
- T010 → T003
- T011 → T004
- T012 → T004, T005–T010
- T013 → T003
- T014 → T013
- T015 → T004, T013
- T016 → T014
- T017 → T014
- T018 → T014, T015–T017

---

## Phase 1: Setup

- [ ] T001 Verify existing test baseline passes by running `agdt-test` and `bash scripts/run-pr-checks.sh` (FR-011)

## Phase 2: Foundational — Retry Infrastructure

- [ ] T002 Add `import time` and define module-level retry constants (`_RETRY_MAX_ATTEMPTS`, `_RETRY_INITIAL_DELAY_S`, `_RETRY_BACKOFF_FACTOR`, `_RETRY_MAX_DELAY_S`) in
  `agentic_devtools/cli/copilot/auto_start.py` (FR-012)
- [ ] T003 Implement `_is_retryable_win_error(exc)` and `_run_copilot_with_retry(copilot_args, cwd)` functions in `agentic_devtools/cli/copilot/auto_start.py`, and replace the bare `subprocess.run`
  call in `copilot_auto_start_cmd` with a call to `_run_copilot_with_retry` (FR-001, FR-002, FR-003, FR-006, FR-008)

## Phase 3: User Story 1 — Retry on Transient Windows File Lock (P1)

- [ ] T004 [P] [US1] Create `tests/unit/cli/copilot/auto_start/test__is_retryable_win_error.py` with tests for: `winerror=32` → True, `winerror=5` → False, no `winerror` attr → False,
  `FileNotFoundError` → False, `winerror=None` → False (FR-001, FR-005)
- [ ] T005 [P] [US1] Create `tests/unit/cli/copilot/auto_start/test__run_copilot_with_retry.py` — success on first try (no retry needed) (FR-002, FR-003)
- [ ] T006 [P] [US1] Add tests in `test__run_copilot_with_retry.py` for transient failure then success after 1, 2, and 5 retries; assert correct total call count, exit code 0, and backoff delays (0.5,
  1.0, 2.0, 4.0, 4.0) (FR-002, FR-003, FR-006)
- [ ] T007 [P] [US1] Add test in `test_copilot_auto_start_cmd.py` for integration: `winerror=32` OSError triggers retry and succeeds after transient failure, exit code 0 (FR-001, FR-002, FR-011)

## Phase 4: User Story 2 — Persistent Failure Diagnostics (P2)

- [ ] T008 [P] [US2] Add tests in `test__run_copilot_with_retry.py` for retry exhaustion after 6 total tries: raises last `OSError`, verify final failure log contains retry count summary and attempt
  details (FR-004, FR-007)
- [ ] T009 [P] [US2] Add test in `test_copilot_auto_start_cmd.py` for integration: `winerror=32` exhaustion → exit 1, `_unmark_run_triggered` called (FR-004, FR-007)
- [ ] T010 [P] [US2] Add test in `test__run_copilot_with_retry.py` for `KeyboardInterrupt` during `time.sleep` — propagates immediately, no further retries; add integration test in
  `test_copilot_auto_start_cmd.py` for exit code 130 and `_unmark_run_triggered` called (FR-009)

## Phase 5: User Story 3 — No Regression on Non-Windows / Non-Retryable Paths (P3)

- [ ] T011 [P] [US3] Add tests in `test__run_copilot_with_retry.py` for: non-retryable `OSError` (no `winerror` attr) fails immediately with no retry, `OSError` with `winerror=5` fails immediately,
  `FileNotFoundError` fails immediately, error type change mid-retry (`winerror=32` → `FileNotFoundError` on attempt 3) (FR-005, FR-010)
- [ ] T012 [P] [US3] Verify existing `OSError` and `FileNotFoundError` tests in `test_copilot_auto_start_cmd.py` still pass unchanged (non-retryable errors bypass retry loop) (FR-005, FR-011)

## Phase 6: Cleanup Warning Logging (FR-013)

- [ ] T013 [P] Add test in `test_copilot_auto_start_cmd.py` for: `[WinError 32]` during `_cleanup_auto_start_task` is logged as a warning to stderr and does not change exit code from 0 (FR-013)
- [ ] T014 Implement warning logging in `_cleanup_auto_start_task` for `[WinError 32]` — catch `OSError` with `winerror=32`, log warning with `agdt-copilot-auto-start:` prefix, and continue
  without affecting exit code (FR-013)

## Phase 7: Polish & Cross-Cutting

- [ ] T015 Run `python scripts/validate_test_structure.py` to verify 1:1:1 compliance for new test files (FR-011)
- [ ] T016 Run `ruff check . && ruff format --check .` to verify lint compliance (FR-011)
- [ ] T017 Run full test suite via `agdt-test` + `agdt-task-wait` and confirm all tests pass (FR-011)
- [ ] T018 Run `bash scripts/run-pr-checks.sh` to confirm all CI-blocking checks pass (FR-011)

---
*Generated by Copilot SDK (claude-opus-4.6)*
