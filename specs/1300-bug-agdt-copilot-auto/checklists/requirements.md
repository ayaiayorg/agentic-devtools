# Specification Quality Checklist: [Bug] agdt-copilot-auto-start.exe intermittent WinError 32 retry resilience

**Purpose**: Validate specification completeness before proceeding to planning
**Created**: 2026-05-03
**Feature**: [spec.md](../spec.md)
**Source Issue**: #1300

## Content Quality

- [ ] CHK001 All four user scenarios (transient retry success, persistent failure diagnostics, clean interruption, no regression) clearly articulate the value to the end user rather than describing
  implementation mechanics
- [ ] CHK002 Each user scenario follows the pattern: actor → trigger condition → expected resilient behavior (e.g., "A user starts an AI workflow in a Windows worktree…the command should retry
  automatically")
- [ ] CHK003 Priority assignments (P1 for transient retry, P2 for diagnostics and interruption, P3 for no-regression) reflect correct relative severity for a bug fix targeting Windows-only
  intermittent failures
- [ ] CHK004 Functional requirements 1–13 describe observable behaviors (retry, stop, emit logging, exit with code) without prescribing internal class hierarchies, decorator patterns, or specific
  function signatures beyond the existing call sites

## Requirement Completeness

- [ ] CHK005 User Scenario 1 (transient retry success) includes a concrete test method: mock `subprocess.run` to raise `OSError(winerror=32)` for N calls (1 ≤ N ≤ 5) then succeed, assert exit
  code 0 and exactly N+1 total invocations
- [ ] CHK006 Edge cases document all six boundary conditions: Ctrl+C during sleep, file/worktree deletion mid-retry, non-Windows platform, unrelated file contention, exhaustion on 6th attempt, and
  concurrent auto-start processes
- [ ] CHK007 User Scenario 2 (persistent failure) specifies acceptance verification: final output includes total retry count, cumulative time, and terminal exception message; exit code is 1;
  `_unmark_run_triggered` is called
- [ ] CHK008 Success Criteria 1–5 each define a measurable pass/fail condition (e.g., "succeeds after one or more retries", "fails only after 5 retries", "exits with code 130")
- [ ] CHK009 Scope is bounded to the `subprocess.run` call site in `copilot_auto_start_cmd` and its surrounding `OSError` handler; state-file operations using `locked_state_file` and the
  best-effort cleanup phase are explicitly excluded from retry wrapping
- [ ] CHK010 Clarifications document all five dependency assumptions: retry wraps only `subprocess.run`, constants are hard-coded module-level values, cleanup phase is not retried, no platform gate
  needed, and `locked_state_file` already handles state-file locking

## Feature Readiness

- [ ] CHK011 All 13 functional requirements have corresponding acceptance coverage: requirements 1–7 map to User Scenarios 1–2 and Success Criteria 1–2; requirements 8–10 map to edge cases;
  requirement 9 maps to User Scenario 3 and Success Criterion 3; requirements 11–13 map to User Scenario 4 and Success Criterion 4
- [ ] CHK012 User Scenario 4 (no regression) covers both non-Windows platforms (`OSError` without `winerror` attribute) and Windows without contention (`winerror != 32`), ensuring the full
  cross-platform matrix is specified
- [ ] CHK013 Success Criterion 5 defines a qualitative but verifiable outcome: each retry log line must include attempt number, delay duration, and winerror code — reviewable via log inspection or
  test assertion on stderr output
- [ ] CHK014 Retry constants (`_RETRY_MAX_ATTEMPTS = 5`, `_RETRY_INITIAL_DELAY_S = 0.5`, `_RETRY_BACKOFF_FACTOR = 2.0`, `_RETRY_MAX_DELAY_S = 4.0`) are intentionally prescribed with exact names in
  requirements 4 and 12 for maintainability; the spec is deliberately prescriptive about these identifiers but does not prescribe internal function decomposition beyond identifying the
  call site to wrap

## Notes

- This checklist was generated from the specification content for issue #1300
- Items marked incomplete require spec updates before proceeding to planning

---
*Generated by Copilot SDK (claude-opus-4.6)*
