# Specification Quality Checklist: Pin agentic-devtools Version in project.json and Guard agdt-setup Against Older Versions

**Purpose**: Validate specification completeness before proceeding to planning
**Created**: 2026-05-05
**Feature**: [spec.md](../spec.md)
**Source Issue**: #1324

## Content Quality

- [ ] CHK001 All four user stories clearly articulate the user value (version pinning prevents teammate regressions, version guard prevents accidental downgrades, force flag enables local-only work,
  clear messaging reduces support burden)
- [ ] CHK002 User stories follow "As a developer… I want… so that…" format consistently across Stories 1–4
- [ ] CHK003 Priority assignments (P1 for Stories 1–2, P2 for Stories 3–4) include justification explaining why version pinning and guard are foundational while force-override and messaging are
  enhancements
- [ ] CHK004 Requirements FR-001 through FR-013 specify observable behaviour (what the system MUST do) without prescribing internal class/function design patterns beyond placement constraints

## Requirement Completeness

- [ ] CHK005 Each acceptance scenario in Stories 1–4 is independently testable with explicit Given/When/Then structure and verifiable exit conditions (exit codes, file contents, stderr output)
- [ ] CHK006 Edge cases section covers malformed `agdt_version`, missing `project.json`, pre-release version comparison, flag combinations (`--force-old-version` + `--skip-pr-workflow`,
  `--force-old-version` + `--system-only`)
- [ ] CHK007 All acceptance scenarios specify concrete version numbers (e.g., `0.2.69` vs `0.2.64`) and expected file state after execution
- [ ] CHK008 Success criteria SC-001 through SC-006 define measurable verification methods (e.g., "verifiable via `git status`", "100% test coverage", "`run-pr-checks.sh` passes")
- [ ] CHK009 Scope boundaries are defined: version guard applies only to `agdt-setup` (not other `agdt-*` commands), `.gitignore` negation is limited to `project.json` path, and `--force-old-version`
  only affects repo-modifying steps
- [ ] CHK010 Dependencies documented: explicit `packaging` library requirement in `pyproject.toml`, dependency on existing `load_project_config`/`save_project_config` pattern, dependency on
  `agentic_devtools.__version__` availability

## Feature Readiness

- [ ] CHK011 Every functional requirement (FR-001 through FR-013) has at least one corresponding acceptance scenario in User Stories 1–4 that validates its behaviour
- [ ] CHK012 User scenarios cover all four personas: first-time setup user (Stories 1–2/FR-001, FR-010), developer with older version (Story 2/FR-004), developer needing local-only setup
  (Story 3/FR-007), and developer reading error output (Story 4/FR-005)
- [ ] CHK013 Success criteria SC-001 through SC-003 specify exact verification commands (`git status` for zero modifications, exit code checks) and SC-004 references the 1:1:1 test structure policy
  quantitatively
- [ ] CHK014 NFR-001 placement constraint ("after `git_root` detection, before `_run_file_modifying_steps()`") and FR-001 implementation note are architectural guidance rather than leaked
  implementation details — verify they remain necessary constraints vs. prescriptive code design

## Notes

- This checklist was generated from the specification content for issue #1324
- Items marked incomplete require spec updates before proceeding to planning
- The `contracts/` directory referenced in the PR description is intentionally excluded from version control (via `specs/*/contracts/` in `.gitignore`) and will not appear in-tree

---
*Generated by Copilot SDK (claude-opus-4.6)*
