# Specification Quality Checklist: Issue type config in project.json with validation/defaults

**Purpose**: Validate specification completeness before proceeding to planning
**Created**: 2026-06-08
**Feature**: [spec.md](../spec.md)
**Source Issue**: #1833

## Content Quality

- [ ] CHK001 User Story 1 (Default Issue Type Fallback) clearly articulates value: eliminating ambiguity when no explicit type is provided, reducing friction in every workflow run
- [ ] CHK002 All three user stories follow the format: actor context, need, and system behavior expectation (AI agent at commit step, developer providing invalid type, team lead during setup)
- [ ] CHK003 Priority assignments (P1 default fallback, P2 validation, P3 documentation) include explicit justification explaining why each level was chosen
- [ ] CHK004 FR-001 through FR-007 describe observable system behavior (reads config, emits warnings, resolves type) without prescribing internal data structures or algorithms beyond module placement

## Requirement Completeness

- [ ] CHK005 Each acceptance scenario in User Stories 1–3 uses Given/When/Then format with verifiable assertions (specific type strings returned, warnings emitted or absent)
- [ ] CHK006 Edge cases section covers: default not in allowed list, empty array for availableCommitIssueTypes, empty string defaultCommitIssueType, and non-string/non-array malformed values
- [ ] CHK007 Acceptance scenarios specify exact observable outputs: type string returned,
  warning text format (single-line: `Warning: Issue type '<type>' is not in availableCommitIssueTypes. Allowed: ['item1', 'item2']`;
  quoted `<type>` and entries use deterministic escaping in FR-004 order (first `\`→`\\`, then `'`→`\'`);
  if >20 entries, show first 19 entries and append `'and N more'` as the final single-quoted list entry so the list remains capped at 20),
  and stderr as output channel
- [ ] CHK008 SC-001 specifies measurable target (100% branch coverage), SC-004 specifies minimum test count (≥8 tests), and NFR-001 specifies latency bound (≤5ms)
- [ ] CHK009 Scope boundaries explicitly state tracker-derived mapping (from issueManagement.issueType/jira.issue_type) is out of scope for #1833 and defined in specs/1829
- [ ] CHK010 Dependencies documented: depends on existing `load_project_config()` in `project_config.py`, relates to `specs/1829-jinja2-commit-message-template/spec.md` for
  `versionControl.commitMessageType` input

## Feature Readiness

- [ ] CHK011 FR-001 through FR-007 each have corresponding acceptance scenarios or edge cases that verify their behavior (config reading, resolution priority, validation, setup idempotency)
- [ ] CHK012 User scenarios cover all three actor types: AI agent in automated workflow (Story 1), developer/agent providing invalid type (Story 2), and team lead configuring project (Story 3)
- [ ] CHK013 Success criteria SC-001 (100% branch coverage), SC-002 (zero spurious warnings), SC-004 (≥8 unit tests), and SC-005 (≥2 documentation locations) are all numerically measurable
- [ ] CHK014 NFR-003 specifies module location (`commit_type_resolution.py`) and function signature (`project_config: dict | None` parameter) but these serve testability constraints rather than
  leaking arbitrary implementation choices into requirements

## Notes

- This checklist was generated from the specification content for issue #1833
- Items marked incomplete require spec updates before proceeding to planning

---
*Generated by Copilot SDK (claude-opus-4.6)*
