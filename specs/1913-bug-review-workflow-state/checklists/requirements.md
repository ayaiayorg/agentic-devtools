# Specification Quality Checklist: PR review workflow state directory mismatch between auto-execute subprocess and VS Code auto-start task

**Purpose**: Validate specification completeness before proceeding to planning
**Created**: 2026-06-06
**Feature**: [spec.md](../spec.md)
**Source Issue**: #1913

## Content Quality

- [ ] CHK001 All three user stories focus on user/developer value (workflow continuity, path stability, backward compatibility) rather than implementation mechanics
- [ ] CHK002 User stories follow "As a [role], I need [capability]" format with explicit priority, independent test, and acceptance scenarios
- [ ] CHK003 Priority assignments (P1 for Stories 1 & 2, P2 for Story 3) reflect business impact — P1 for the blocking bug and divergence, P2 for regression safety
- [ ] CHK004 Functional requirements FR-001 through FR-008 describe observable behaviors (pin file consultation, canonical path selection); where they reference `pinned-state-dir.json` and specific
  functions (FR-003/FR-007), this is intentional — the requirements lean on the existing pin-file contract as a behavioral interface, not as an internal implementation prescription

## Requirement Completeness

- [ ] CHK005 Acceptance scenarios for User Story 1 are independently testable by running `@agdt.advance-workflow` after auto-execute without manual state repair
- [ ] CHK006 Edge cases document all failure modes: expired pin file TTL, deleted state directory, stale `AGENTIC_DEVTOOLS_STATE_DIR`, missing bootstrap metadata, case-insensitive path collisions, and
  concurrent pin file writes
- [ ] CHK007 Acceptance scenarios use Given/When/Then format consistently across all three user stories (6 total scenarios)
- [ ] CHK008 Success criteria SC-001 through SC-004 define measurable outcomes: no "no active workflow" error, matching log paths, no split artifacts, and passing existing tests
- [ ] CHK009 Scope boundaries are explicit — the reported P1 reproduction is `pull-request-review`, while the state-resolution contract is described broadly enough for all workflows that use
  worktree auto-setup + auto-execute
- [ ] CHK010 Dependencies on existing infrastructure are documented: `pinned-state-dir.json` mechanism from #1180, `_run_auto_execute_command`, `get_state_dir()`, `read_and_validate_pin_file`, and
  `RECOGNIZED_PIN_WORKFLOWS` frozenset

## Feature Readiness

- [ ] CHK011 All eight functional requirements (FR-001 through FR-008) have corresponding acceptance criteria traceable through User Stories 1–3
- [ ] CHK012 User scenarios cover all three interaction modes: cross-worktree handoff (Story 1), multi-step initialization consistency (Story 2), and single-repo no-handoff regression (Story 3)
- [ ] CHK013 Success criteria SC-001 and SC-002 specify concrete observable outputs (advance-workflow response and matching log paths) rather than subjective assessments
- [ ] CHK014 FR-003 and FR-007 reference specific functions (`_run_auto_execute_command`, `copilot_auto_start_cmd`) and file paths (`pinned-state-dir.json`) as behavioral contracts, not as
  implementation prescriptions — verify these remain at the "what" level

## Notes

- This checklist was generated from the specification content for issue #1913
- Items marked incomplete require spec updates before proceeding to planning

---
*Generated by Copilot SDK (claude-opus-4.6)*
