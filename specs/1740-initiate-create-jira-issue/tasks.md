# Tasks: Create-Jira-Issue Stale State Fix (#1740)

## Phase Mapping: Plan → Tasks

Phases are 1:1 aligned with plan.md — no mapping needed.

## Phase 1: Setup

- [ ] T001 [US1] Scaffold test directory and `__init__.py` for `tests/unit/cli/workflows/commands/test__clear_stale_issue_keys_for_create.py` (FR-001)

## Phase 2: Foundational

- [ ] T002 Verify existing `_ensure_scoped_bootstrap_and_clear` behavior in `agentic_devtools/cli/workflows/commands.py` preserves `jira.issue_key` (confirms root cause, validates FR-007 constraint
  that `clear_state_for_workflow_initiation()` in `agentic_devtools/cli/workflows/base.py` must NOT be modified)

## Phase 3: User Story 1 & 2 — Stale State Clearance (P1)

- [ ] T003 [US1] Write failing tests in `tests/unit/cli/workflows/commands/test__clear_stale_issue_keys_for_create.py` for `_clear_stale_issue_keys_for_create` helper: clears both keys (FR-002),
  clears only `issue_key`, clears only `jira.issue_key`, no-op when no keys exist
- [ ] T004 [US1] Implement `_clear_stale_issue_keys_for_create()` helper function in `agentic_devtools/cli/workflows/commands.py` in the `initiate_create_jira_issue_workflow()` command section — deletes
  `issue_key` and `jira.issue_key` from state
  when present (FR-002, FR-001)
- [ ] T005 [US1] Write failing tests in `tests/unit/cli/workflows/commands/test_initiate_create_jira_issue_workflow.py` for happy-path stale-state scenario: verify stale `jira.issue_key` is
  cleared and create flow successfully proceeds to Jira API for fresh issue (FR-001, FR-003)
- [ ] T006 [US1] Insert guard in `initiate_create_jira_issue_workflow()` in `agentic_devtools/cli/workflows/commands.py` after `_ensure_scoped_bootstrap_and_clear(issue_key)`: call
  `_clear_stale_issue_keys_for_create()` when `issue_key` is falsy (FR-001, FR-002)
- [ ] T007 [US2] Write test verifying `jira.project_key` is preserved after stale issue key clearance in `tests/unit/cli/workflows/commands/test_initiate_create_jira_issue_workflow.py` (FR-002
  scoping)
- [ ] T008 [US2] Write happy-path success test verifying downstream workflow uses only the newly created issue key (not stale key) in
  `tests/unit/cli/workflows/commands/test_initiate_create_jira_issue_workflow.py` (FR-004)
- [ ] T009 [US1] Write test verifying no context mismatch warnings are emitted for stale key during create flow in `tests/unit/cli/workflows/commands/test_initiate_create_jira_issue_workflow.py`
  (FR-003, FR-004)

## Phase 4: User Story 3 — Observable Stale-State Handling (P2)

- [ ] T010 [US3] Write failing tests in `tests/unit/cli/workflows/commands/test__clear_stale_issue_keys_for_create.py` for stderr message: format matches emoji-prefixed pattern, lists cleared key
  names (FR-005)
- [ ] T011 [US3] Write test in `tests/unit/cli/workflows/commands/test_initiate_create_jira_issue_workflow.py` verifying stderr message is emitted when stale state detected during workflow initiation
  (FR-005)
- [ ] T012 [P] [US3] Write test verifying no stderr message when no stale keys exist in `tests/unit/cli/workflows/commands/test__clear_stale_issue_keys_for_create.py` (FR-005 negative case)

## Phase 5: User Story 4 — Explicit --issue-key Preserves Behavior (P1)

- [ ] T013 [US4] Write happy-path nominal test verifying explicit `--issue-key` bypasses stale-state clearing and uses provided key in
  `tests/unit/cli/workflows/commands/test_initiate_create_jira_issue_workflow.py` (FR-006)
- [ ] T014 [US4] Write test verifying no stale-state warning emitted when `--issue-key` is explicitly provided in `tests/unit/cli/workflows/commands/test_initiate_create_jira_issue_workflow.py`
  (FR-006)

## Phase 6: Polish & Cross-Cutting

- [ ] T015 Run targeted checks (`bash scripts/targeted-checks.sh`) to ensure lint, format, mypy, and per-file coverage pass for `agentic_devtools/cli/workflows/commands.py`
- [ ] T016 Run full suite (`agdt-test` + `agdt-task-wait`) to confirm no regressions
- [ ] T017 Verify happy-path FR-007 compliance: confirm no changes were made to `agentic_devtools/cli/workflows/base.py` (`clear_state_for_workflow_initiation` unchanged)

---
*Generated by Copilot SDK (claude-opus-4.6)*
