# Specification Quality Checklist: Remove active_session gate from dispatch_repair and use gh agent-task for reliable session detection

**Purpose**: Validate specification completeness before proceeding to planning
**Created**: 2026-05-29
**Feature**: [spec.md](../spec.md)
**Source Issue**: #1643

## Content Quality

- [ ] CHK001 All four user stories clearly articulate user value (automation operator unblocked, developer gets reliable detection, fail-open safety, migration correctness) rather than describing
  implementation mechanics
- [ ] CHK002 Each user story follows "As a [role], I need [capability], so that [benefit]" format — verify User Stories 1-4 each have explicit role, need, and rationale
- [ ] CHK003 Priority assignments (P1 for dispatch gate removal, P2 for new detector/fail-open/migration) are justified with explicit reasoning in "Why this priority" sections
- [ ] CHK004 Functional requirements FR-001 through FR-008 specify WHAT behavior is required without prescribing internal algorithms, data structures, or code patterns beyond interface contracts

## Requirement Completeness

- [ ] CHK005 All acceptance scenarios in User Stories 1-4 use Given/When/Then format with concrete, testable assertions (e.g., specific `ActionDecision` enum values, specific return types)
- [ ] CHK006 Edge cases section covers multi-session overlap, older gh CLI without agent-task subcommand, concurrent dispatch+active session, long-running stuck sessions, and near-timeout subprocess
  scenarios
- [ ] CHK007 Each acceptance scenario specifies exact input conditions (snapshot field values, gh output states) and expected outputs (decision enum, boolean return, log behavior)
- [ ] CHK008 Success criteria SC-001 through SC-006 each include a numeric target (0 occurrences, 100% fixture pass, 5/5 failure modes, 0 seconds delay, 0 test failures, zero snapshot reads)
- [ ] CHK009 Scope boundaries are explicit: dispatch_repair loses gate entirely, SquashAction/RequestReviewAction migrate to new detector, summary renderer updates — verify no ambiguity about what is
  in/out of scope
- [ ] CHK010 Dependencies on `gh` CLI availability and `agent-task` subcommand existence are documented; assumption that `gh agent-task list` returns JSON with `id`, `status`, `pullRequestNumber`,
  `createdAt` fields is explicitly stated in FR-003

## Feature Readiness

- [ ] CHK011 Every functional requirement (FR-001 through FR-008) maps to at least one acceptance scenario in User Stories 1-4 or edge cases — verify FR-006 (DeprecationWarning emission) and FR-007
  (snapshot field retention) have testable acceptance criteria
- [ ] CHK012 User scenario coverage spans all affected components: DispatchRepairAction (Story 1), new detector function (Story 2), error handling (Story 3), SquashAction and RequestReviewAction
  migration (Story 4), and summary renderer (FR-008e)
- [ ] CHK013 Success criteria SC-004 (0 seconds session-related delay) is measurable via test assertions rather than requiring production observation; SC-002 defines "fixture set" but verify the
  fixture scenarios are enumerable from acceptance scenarios
- [ ] CHK014 Requirements do not prescribe internal implementation details beyond the public function signature in FR-003, the file location in FR-003/FR-006, and the recognized active status values
  in FR-005 — verify these are intentional interface contracts, not leaked implementation

## Notes

- This checklist was generated from the specification content for issue #1643
- Items marked incomplete require spec updates before proceeding to planning

---
*Generated by Copilot SDK (claude-opus-4.6)*
