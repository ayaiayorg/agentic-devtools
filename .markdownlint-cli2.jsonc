// markdownlint-cli2 configuration for agentic-devtools
{
  "config": {
    "$schema": "https://raw.githubusercontent.com/DavidAnson/markdownlint/main/schema/markdownlint-config-schema.json",
    "default": true,
    // MD013: Line length - increase limit to 200 for documentation readability
    // Exclude code blocks and tables which often need longer lines
    "MD013": {
      "line_length": 200,
      "code_blocks": false,
      "tables": false
    },
    // MD041: First line in file should be top-level heading
    // Files with YAML front matter are exempt (regex matches any front matter content)
    "MD041": {
      "front_matter_title": ".*"
    },
    // MD060: Table column style - disabled because table formatting is often
    // auto-generated and forcing alignment can make content harder to maintain
    "MD060": false,
    // MD036: Emphasis as heading - disable because we use emphasis for subheadings
    // in documentation which is acceptable for our style
    "MD036": false,
    // MD024: Allow duplicate headings under different parent sections
    "MD024": {
      "siblings_only": true
    },
    // MD029: Ordered list item prefix - allow sequential numbering (1, 2, 3)
    "MD029": {
      "style": "ordered"
    },
    // MD033: Inline HTML - allow common HTML elements used in markdown
    "MD033": {
      "allowed_elements": ["a", "img", "br", "details", "summary"]
    }
  },
  "customRules": [],
  "globs": [
    "**/*.md"
  ],
  // GitHub issue templates use YAML front matter + ## headings (not # h1),
  // which conflicts with MD041. Exclude them from linting.
  // Agent files follow their own conventions and should not be linted.
  "ignores": [".github/ISSUE_TEMPLATE/**", ".github/agents/**"]
}
